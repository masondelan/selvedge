{
  "manifest_version": "0.3",
  "name": "selvedge",
  "display_name": "Selvedge",
  "version": "0.3.13",
  "description": "Decision provenance for AI-coded codebases: the why, and what was already tried and rejected. A local MCP server: agents call `prior_attempts` before editing an entity — \"was this tried here before, and how did it turn out?\" — and the trail reads tried → reverted → re-opened, because a reverted decision is not a permanent ban. Entity-level rather than line-level: DB columns, functions, env vars, dependencies, API routes. An append-only, queryable audit trail — a git blame for decisions. Local SQLite, and no LLM anywhere in the storage or retrieval path, so the same query returns the same answer in two years.",
  "long_description": "Selvedge is an MCP server that AI coding agents call as they work to log structured change events. It captures the entity (DB column, table, function, endpoint, dependency, env var), the diff, and the reasoning — the intent that would otherwise evaporate when the agent session ends.\n\nWith human-written code, intent leaked into commit messages and PR descriptions. With AI-written code, intent lives in a prompt that disappears. Selvedge writes it down before it's gone, so weeks later you can ask 'when was users.stripe_customer_id added and why?' and get a real answer.\n\nThe SQLite DB lives locally in `.selvedge/` next to your code — your data stays yours.",
  "author": {
    "name": "Mason Delan",
    "email": "masondelan@gmail.com",
    "url": "https://github.com/masondelan"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/masondelan/selvedge.git"
  },
  "homepage": "https://selvedge.sh",
  "documentation": "https://selvedge.sh",
  "support": "https://github.com/masondelan/selvedge/issues",
  "license": "MIT",
  "keywords": [
    "mcp",
    "ai",
    "changelog",
    "codebase",
    "tracking",
    "devtools",
    "git",
    "blame",
    "history",
    "changeset",
    "audit",
    "ai-agents",
    "claude-code",
    "cursor",
    "copilot"
  ],
  "server": {
    "type": "python",
    "entry_point": "selvedge/server.py",
    "mcp_config": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${__dirname}",
        "python",
        "-m",
        "selvedge.server"
      ]
    }
  },
  "tools": [
    {
      "name": "log_change",
      "title": "Log a code change",
      "description": "Record a change to a codebase entity.\n\nCall this immediately after making any meaningful change. The event is\nwritten to the local SQLite store and returned with its assigned id and\ntimestamp. If the reasoning fails the quality validator (empty, too\nshort, or a generic placeholder), or the entity_path doesn't match the\nusual shape for its entity_type, the result includes a `warnings`\narray — the event is still stored.\n\nRenames: pass the new path in `entity_path`, set `change_type=\"rename\"`,\nand pass the old path in `rename_from`. Selvedge then writes two events —\na `rename` on the old path and a `create` on the new path with\n`metadata.renamed_from` set — so the entity's history follows it. Example:\n\n    log_change(\n        entity_path=\"src/auth/session.py::login\",   # new path\n        change_type=\"rename\",\n        rename_from=\"src/auth.py::login\",            # old path\n        entity_type=\"function\",\n        reasoning=\"Split auth.py into an auth/ package; login moved.\",\n    )\n\nRejections: when you consider an approach and decide against it WITHOUT\nwriting the change, record the verdict with `change_type=\"reject\"` — the\nabandoned path is a first-class event, and the next agent's\n`prior_attempts` query finds it as a high-confidence (\"exact\") row\ninstead of re-deriving the dead end. Name what was rejected AND what was\nchosen instead, and record the condition that would invalidate the\nverdict. Example:\n\n    log_change(\n        entity_path=\"users.card_pan\",\n        change_type=\"reject\",\n        entity_type=\"column\",\n        reasoning=\"Rejected storing raw card PANs on the user row — \"\n                  \"went with provider tokens instead; PANs in our own \"\n                  \"DB put us in PCI scope.\",\n        stale_when=\"payment provider changed\",\n        expires_when=\"entity:deps/stripe:changes\",\n    )\n\nUse `change_type=\"revert\"` for the sibling case — the change WAS written\nand then rolled back (clearer than a plain remove).\n\nSuperseding a reverted decision: when a reverted change becomes correct\nagain (the constraint that killed it no longer holds), do NOT delete or\nedit history — log with `change_type=\"supersede\"` and the reason. The\nnew event links the prior revert (auto-resolved when `supersedes` is\nempty) and every read surface then reports the trail\ntried → reverted → re-opened. Never re-apply a reverted change without\nsuperseding it first.\n\nOn validation failure (invalid change_type, missing entity_path,\n`rename_from` set without change_type='rename', `supersedes` set without\nchange_type='supersede', a supersede with nothing to re-open, or an\n`expires_when` outside the closed grammar) the result is\n`{\"status\": \"error\", \"error\": \"...\"}` with no event written.",
      "inputSchema": {
        "properties": {
          "entity_path": {
            "description": "Dot/slash-notation path to the entity. Required and non-empty. Examples: 'users.email' (DB column), 'users' (DB table), 'src/auth.py::login' (function in file), 'src/auth.py' (file), 'api/v1/users' (API route), 'deps/stripe' (dependency), 'env/STRIPE_SECRET_KEY' (env variable).",
            "title": "Entity Path",
            "type": "string"
          },
          "change_type": {
            "description": "What kind of change. One of: add, remove, modify, rename, retype, create, delete, index_add, index_remove, migrate, revert (tried and rolled back), reject (considered and decided against, without writing the change), supersede (re-open a reverted decision). Invalid values are rejected — pick the closest match.",
            "title": "Change Type",
            "type": "string"
          },
          "diff": {
            "default": "",
            "description": "The actual change — SQL migration text, code diff, or a human-readable description of what changed. Optional but strongly recommended for non-trivial changes.",
            "title": "Diff",
            "type": "string"
          },
          "entity_type": {
            "default": "other",
            "description": "Category of entity. One of: column, table, file, function, class, endpoint, dependency, env_var, index, schema, config, other. Unknown values are coerced to 'other'.",
            "title": "Entity Type",
            "type": "string"
          },
          "reasoning": {
            "default": "",
            "description": "Why the change was made. Include the user's original request, the problem being solved, or any context that won't be obvious from the diff alone. Good example: 'User asked to add 2FA — needs phone number to send SMS verification codes.' Avoid generic placeholders like 'user request' or 'done' — these are flagged by the quality validator and returned in `warnings`.",
            "title": "Reasoning",
            "type": "string"
          },
          "agent": {
            "default": "",
            "description": "Name/ID of the AI agent making the change (e.g. 'claude-code', 'cursor', 'copilot', 'human').",
            "title": "Agent",
            "type": "string"
          },
          "session_id": {
            "default": "",
            "description": "The agent session or conversation ID, if available.",
            "title": "Session Id",
            "type": "string"
          },
          "git_commit": {
            "default": "",
            "description": "The git commit hash this change will land in. Can be backfilled later via `selvedge backfill-commit` or the post-commit hook.",
            "title": "Git Commit",
            "type": "string"
          },
          "project": {
            "default": "",
            "description": "Repository or project name. Useful when one DB tracks multiple projects.",
            "title": "Project",
            "type": "string"
          },
          "changeset_id": {
            "default": "",
            "description": "Optional grouping ID for related changes that belong to the same feature or task. Use a short slug like 'add-stripe-billing'. All events sharing a changeset_id can be queried together via the `changeset` tool.",
            "title": "Changeset Id",
            "type": "string"
          },
          "revisit_after": {
            "default": "",
            "description": "Optional revisit date for an architectural decision (table, schema, dependency, config). An ISO date OR a relative offset from this event's timestamp (e.g. '90d', '6mo'). `stale_decisions` surfaces it once it passes, if the entity is still in active use. Leave empty otherwise.",
            "title": "Revisit After",
            "type": "string"
          },
          "expires_when": {
            "default": "",
            "description": "Optional machine-checkable expiry condition for this decision. Closed grammar, validated at write time: 'library:NAME>=VERSION' (revisit when the named dependency reaches a version, e.g. 'library:django>=5.0'), 'entity:PATH:changes' (revisit when that entity next changes, e.g. 'entity:users.email:changes'), 'date:ISO' (revisit on a date, e.g. 'date:2027-01-01'), or 'manual:LABEL' (opaque label for human review; never auto-fires). `stale_decisions` evaluates these from local state — no network, no LLM — and flags 'expired' with the pattern that fired. Values outside the grammar are rejected.",
            "title": "Expires When",
            "type": "string"
          },
          "constraint": {
            "default": "",
            "description": "Optional: the testable principle behind the decision, kept queryable (e.g. 'card data in our own DB = PCI scope').",
            "title": "Constraint",
            "type": "string"
          },
          "stale_when": {
            "default": "",
            "description": "Optional: what would invalidate this decision (e.g. 'payment provider changed'). stale_decisions matches it against later events and flags 'review suggested' — surfacing only.",
            "title": "Stale When",
            "type": "string"
          },
          "supersedes": {
            "default": "",
            "description": "Id of the prior event this change overrides; only valid with change_type='supersede'. Empty auto-links the entity's most recent removal event (remove/delete/index_remove/revert/reject) — so after a standalone rejection it re-opens the rejection. Append-only — the old verdict is never edited, just derived as superseded.",
            "title": "Supersedes",
            "type": "string"
          },
          "rename_from": {
            "default": "",
            "description": "The entity's previous path, when this change is a rename. Set it together with change_type='rename' and put the NEW path in entity_path. Selvedge records the dual-event rename pattern: a 'rename' event on the old path and a 'create' event on the new path whose metadata.renamed_from points back to the old one, so blame/diff/prior_attempts on the new path still see the history. Leave empty for any non-rename change.",
            "title": "Rename From",
            "type": "string"
          }
        },
        "required": [
          "entity_path",
          "change_type"
        ],
        "title": "log_changeArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "id": {
            "title": "Id",
            "type": "string"
          },
          "timestamp": {
            "title": "Timestamp",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "error": {
            "title": "Error",
            "type": "string"
          },
          "warnings": {
            "items": {
              "type": "string"
            },
            "title": "Warnings",
            "type": "array"
          },
          "supersedes": {
            "title": "Supersedes",
            "type": "string"
          }
        },
        "required": [
          "id",
          "timestamp",
          "status",
          "error",
          "warnings",
          "supersedes"
        ],
        "title": "LogChangeResult",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "diff",
      "title": "Diff an entity's history",
      "description": "Get change history for a codebase entity, newest first.\n\nSupports prefix matching — e.g. 'users' returns all events for the users\ntable and any users.* column. Each event carries a derived\n`superseded_by` id (\"\" when nothing overrode it), so the\ntried → reverted → re-opened trail reads straight off the history.",
      "inputSchema": {
        "properties": {
          "entity_path": {
            "description": "Entity path, or a DOTTED prefix of one: 'users' also covers 'users.email'. Not a raw string prefix — 'src/' matches nothing, and 'src/auth.py' does not cover 'src/auth.py::login'.",
            "title": "Entity Path",
            "type": "string"
          },
          "limit": {
            "default": 20,
            "description": "Maximum number of events to return.",
            "maximum": 1000,
            "minimum": 1,
            "title": "Limit",
            "type": "integer"
          }
        },
        "required": [
          "entity_path"
        ],
        "title": "diffArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "result": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Result",
            "type": "array"
          }
        },
        "required": [
          "result"
        ],
        "title": "diffOutput",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "blame",
      "title": "Blame an entity",
      "description": "Most recent change to an entity — what changed, when, who, why.\n\nLike `git blame` but for semantic entities (DB columns, functions, env\nvars, dependencies) and AI agents. Also carries the derived decision\nstate: `status` (`active` / `reverted` / `reopened`) and\n`superseded_by` (id of a later supersede overriding this change, or\n\"\"). If no history exists for the entity, returns `{\"error\": \"...\"}`\nwith protocol-level `isError: false`.",
      "inputSchema": {
        "properties": {
          "entity_path": {
            "description": "Exact entity path (no prefix matching). Examples: 'users.email', 'src/auth.py::login', 'env/STRIPE_SECRET_KEY'.",
            "title": "Entity Path",
            "type": "string"
          }
        },
        "required": [
          "entity_path"
        ],
        "title": "blameArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "id": {
            "title": "Id",
            "type": "string"
          },
          "timestamp": {
            "title": "Timestamp",
            "type": "string"
          },
          "entity_type": {
            "title": "Entity Type",
            "type": "string"
          },
          "entity_path": {
            "title": "Entity Path",
            "type": "string"
          },
          "change_type": {
            "title": "Change Type",
            "type": "string"
          },
          "diff": {
            "title": "Diff",
            "type": "string"
          },
          "reasoning": {
            "title": "Reasoning",
            "type": "string"
          },
          "agent": {
            "title": "Agent",
            "type": "string"
          },
          "session_id": {
            "title": "Session Id",
            "type": "string"
          },
          "git_commit": {
            "title": "Git Commit",
            "type": "string"
          },
          "project": {
            "title": "Project",
            "type": "string"
          },
          "changeset_id": {
            "title": "Changeset Id",
            "type": "string"
          },
          "metadata": {
            "additionalProperties": true,
            "title": "Metadata",
            "type": "object"
          },
          "revisit_after": {
            "title": "Revisit After",
            "type": "string"
          },
          "expires_when": {
            "title": "Expires When",
            "type": "string"
          },
          "supersedes": {
            "title": "Supersedes",
            "type": "string"
          },
          "constraint": {
            "title": "Constraint",
            "type": "string"
          },
          "stale_when": {
            "title": "Stale When",
            "type": "string"
          },
          "superseded_by": {
            "title": "Superseded By",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "error": {
            "title": "Error",
            "type": "string"
          }
        },
        "required": [
          "id",
          "timestamp",
          "entity_type",
          "entity_path",
          "change_type",
          "diff",
          "reasoning",
          "agent",
          "session_id",
          "git_commit",
          "project",
          "changeset_id",
          "metadata",
          "revisit_after",
          "expires_when",
          "supersedes",
          "constraint",
          "stale_when",
          "superseded_by",
          "status",
          "error"
        ],
        "title": "BlameResult",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "history",
      "title": "Browse history",
      "description": "Filtered change history across all entities, newest first.\n\nCombine `since`, `entity_path`, `project`, and `changeset_id` to scope\nthe result. On unparseable `since` input the response is\n`[{\"error\": \"...\"}]` so the caller sees the problem.",
      "inputSchema": {
        "properties": {
          "since": {
            "default": "",
            "description": "Time window — ISO 8601 datetime OR relative shorthand: '15m' (last 15 minutes), '24h' (last 24 hours), '7d' (last 7 days), '5mo' (last 5 months), '1y' (last year). 'm' means minutes; 'mo' or 'mon' means months. Unparseable values produce an error rather than silently returning empty results. Empty = all time.",
            "title": "Since",
            "type": "string"
          },
          "entity_path": {
            "default": "",
            "description": "Filter to an entity, or a DOTTED prefix of one ('users' also covers 'users.email'). Not a raw string prefix.",
            "title": "Entity Path",
            "type": "string"
          },
          "project": {
            "default": "",
            "description": "Filter to a specific project/repository.",
            "title": "Project",
            "type": "string"
          },
          "changeset_id": {
            "default": "",
            "description": "Filter to a specific changeset (feature/task group).",
            "title": "Changeset Id",
            "type": "string"
          },
          "limit": {
            "default": 50,
            "description": "Maximum number of results.",
            "maximum": 1000,
            "minimum": 1,
            "title": "Limit",
            "type": "integer"
          }
        },
        "title": "historyArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "result": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Result",
            "type": "array"
          }
        },
        "required": [
          "result"
        ],
        "title": "historyOutput",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "changeset",
      "title": "Get a changeset",
      "description": "All events that share a `changeset_id`, oldest first.\n\nUse to reconstruct the full scope of a feature or task across multiple\nentities. If the changeset has no events, returns\n`[{\"error\": \"...\"}]` so the caller can distinguish \"unknown changeset\"\nfrom \"empty history.\"",
      "inputSchema": {
        "properties": {
          "changeset_id": {
            "description": "The changeset identifier (the same slug or UUID passed to `log_change`'s changeset_id parameter). Examples: 'add-stripe-billing', 'fix-auth-redirect'.",
            "title": "Changeset Id",
            "type": "string"
          }
        },
        "required": [
          "changeset_id"
        ],
        "title": "changesetArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "result": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Result",
            "type": "array"
          }
        },
        "required": [
          "result"
        ],
        "title": "changesetOutput",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "search",
      "title": "Search events",
      "description": "Full-text search across entity paths, diffs, reasoning, and agents.\n\nUseful for questions like 'what changes were made for the billing\nfeature?', 'which columns were added by cursor?', or 'show everything\nrelated to authentication'.",
      "inputSchema": {
        "properties": {
          "query": {
            "description": "Search string (case-insensitive substring). Searches across entity_path, diff, reasoning, and agent fields. SQL LIKE wildcards (`_` and `%`) are escaped, so 'stripe_customer_id' matches the literal underscore rather than any single char.",
            "title": "Query",
            "type": "string"
          },
          "limit": {
            "default": 20,
            "description": "Maximum number of results.",
            "maximum": 1000,
            "minimum": 1,
            "title": "Limit",
            "type": "integer"
          }
        },
        "required": [
          "query"
        ],
        "title": "searchArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "result": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Result",
            "type": "array"
          }
        },
        "required": [
          "result"
        ],
        "title": "searchOutput",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "prior_attempts",
      "title": "Prior attempts on an entity",
      "description": "Prior change attempts on an entity, each with an inferred outcome.\n\nCall this BEFORE editing an entity. If the same change was tried before\nand reverted, you get the prior `reasoning` and `change_type` plus an\ninferred `outcome` — so you can change your plan instead of repeating a\nrejected approach.\n\nEach result is a change event plus the trail fields: `outcome`\n(\"reverted\" — a later removal on the path; \"reopened\" — closed but a\nlater supersede re-opened it; \"rejected\" — a standalone `reject` event\nthat closed no earlier attempt, surfaced as its own row whose reasoning\nIS the record; \"active\"), `confidence` (\"exact\" — the attempt was closed\nby an explicit revert/reject, or the row is a standalone rejection;\n\"proximity_high\" / \"proximity_low\" — the add->remove window heuristic\nfor implicit removals), `outcome_reasoning` (WHY it was rejected),\n`superseded_by` + `supersede_reasoning` (the re-open, when present), and\n`current_status` — the entity's standing now. Treat \"reverted\" and\n\"rejected\" as \"don't repeat this without a supersede\"; \"reopened\" means\nthe old verdict no longer stands. Together they read: tried → reverted →\nre-opened. Templated and deterministic — no LLM call; pull-only.\n\nConservative by design — `min_confidence` defaults to \"proximity_high\",\nso an empty list (nothing clearly tried-and-rejected) is the normal,\npreferred answer over a speculative false positive; \"exact\" rows always\nclear that default floor. Pass `min_confidence=\"proximity_low\"` to widen\nrecall. Rows carry `match_type` (\"exact\" / \"substring\" / \"fuzzy\") and\n`similarity`.",
      "inputSchema": {
        "properties": {
          "entity_path": {
            "default": "",
            "description": "The entity you're about to change. Exact path with prefix matching — 'users' also covers 'users.email'. Examples: 'src/auth.py::login', 'users.email', 'env/STRIPE_SECRET_KEY'. Provide this OR `description`.",
            "title": "Entity Path",
            "type": "string"
          },
          "description": {
            "default": "",
            "description": "Free-text description of what you're about to do, when you don't have an exact entity_path. Matched as a substring against prior reasoning, diffs, and entity paths. Provide this OR `entity_path` (entity_path takes precedence if both are given).",
            "title": "Description",
            "type": "string"
          },
          "fuzzy": {
            "default": "",
            "description": "Optional semantic query: also return attempts on entities whose prior reasoning is similar to this text — catches renames (payment_token vs card_token). Rows are labeled match_type='fuzzy' with a similarity score; without the selvedge[semantic] extra it falls back to substring matching and says so in a leading note row.",
            "title": "Fuzzy",
            "type": "string"
          },
          "min_confidence": {
            "default": "proximity_high",
            "description": "Confidence floor. 'proximity_high' (default) returns the high-signal rows: attempts closed by an explicit revert/reject (confidence 'exact' — always clears this floor, including standalone rejections) plus attempts reverted within the window. Pass 'proximity_low' to also see the noisy tail (still-active changes and far-apart reverts).",
            "title": "Min Confidence",
            "type": "string"
          },
          "window_minutes": {
            "default": 10080,
            "description": "Proximity window in minutes for the add->remove revert heuristic — the tiebreaker for IMPLICIT removal types only. An attempt removed within this many minutes is 'proximity_high'; beyond it, 'proximity_low'. Attempts closed by an explicit revert/reject are 'exact' regardless of the window. Default 10080 (7 days).",
            "maximum": 1000,
            "minimum": 1,
            "title": "Window Minutes",
            "type": "integer"
          },
          "limit": {
            "default": 20,
            "description": "Maximum number of results.",
            "maximum": 1000,
            "minimum": 1,
            "title": "Limit",
            "type": "integer"
          }
        },
        "title": "prior_attemptsArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "result": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Result",
            "type": "array"
          }
        },
        "required": [
          "result"
        ],
        "title": "prior_attemptsOutput",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "stale_decisions",
      "title": "Stale decisions due for revisit",
      "description": "Decisions due for a revisit — expired, past their date, or with a triggered stale condition.\n\nThree deterministic rules. Expiry-based (`flag=\"expired\"`): events whose\n`expires_when` condition fired, evaluated from local state only —\n`date:` against now, `entity:PATH:changes` against the event log,\n`library:NAME>=VERSION` against installed dist metadata; the pattern\nkind that fired is in `expired_pattern`. A `library:` condition whose\ndependency isn't locally observable surfaces as `flag=\"manual_review\"`\ninstead of a guess; `manual:LABEL` never auto-fires. Date-based\n(`flag=\"revisit_due\"`): events whose `revisit_after` has passed AND the\nentity is still live (queried via `blame`/`diff`/`prior_attempts` after\nthe decision, or its changeset saw later activity) — pure age alone\nnever surfaces. Condition-based (`flag=\"review_suggested\"`): events\nwhose `stale_when` text shares keywords with a LATER change event — the\nnamed invalidation evidence may have happened. Surfacing only: nothing\nis un-retired automatically; follow up with a `supersede` if the\ncondition really was triggered. A later `supersede` that re-opens the\ncandidate (explicit `supersedes` id, or the same id-less auto-link\n`prior_attempts` uses) drops it from this list; a same-path sibling\nthe supersede did not target still surfaces.\n\nEach result is the change event plus `flag`, `revisit_due`,\n`days_overdue`, `active_use_signals`, `matched_terms`,\n`matched_event_id`, `expires_status`, `expired_pattern`,\n`expires_detail`, and a one-line `stale_reason`. Date-due rows first,\nmost-overdue leading; filter by `entity_path`, `project`, or `agent`.\nTemplated and deterministic; no LLM call, no network.",
      "inputSchema": {
        "properties": {
          "entity_path": {
            "default": "",
            "description": "Optional filter to a single entity or path prefix — 'users' also covers 'users.email'. Empty = every entity.",
            "title": "Entity Path",
            "type": "string"
          },
          "project": {
            "default": "",
            "description": "Optional filter to a specific project/repository.",
            "title": "Project",
            "type": "string"
          },
          "agent": {
            "default": "",
            "description": "Optional filter to the agent that logged the decision.",
            "title": "Agent",
            "type": "string"
          },
          "limit": {
            "default": 20,
            "description": "Maximum number of results.",
            "maximum": 1000,
            "minimum": 1,
            "title": "Limit",
            "type": "integer"
          }
        },
        "title": "stale_decisionsArguments",
        "type": "object"
      },
      "outputSchema": {
        "properties": {
          "result": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Result",
            "type": "array"
          }
        },
        "required": [
          "result"
        ],
        "title": "stale_decisionsOutput",
        "type": "object"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    }
  ],
  "compatibility": {
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ],
    "runtimes": {
      "python": ">=3.10"
    }
  },
  "icon": "docs/icon.png"
}
