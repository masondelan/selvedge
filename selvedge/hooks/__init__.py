"""Agent-side enforcement hooks (v0.3.9.1).

The ``selvedge-hook`` console script is the entry point Claude Code invokes;
see :mod:`selvedge.hooks.pretooluse` for the PreToolUse gate that blocks
schema/migration edits until ``prior_attempts`` has been consulted.
"""
