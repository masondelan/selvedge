---
description: git blame for a semantic entity — the who/what/why behind a DB column, function, endpoint, dependency, or env var, with the reasoning that was recorded.
argument-hint: [entity]
allowed-tools: Bash(selvedge-cli:*)
---

Run `selvedge-cli blame "$ARGUMENTS"` and report the entity's change history. Read-only.
