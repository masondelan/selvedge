---
description: Was this change tried and reverted before? Check an entity's prior attempts, and why they were rejected, before editing it.
argument-hint: [entity]
allowed-tools: Bash(selvedge-cli:*)
---

Run `selvedge-cli prior-attempts "$ARGUMENTS"` and report whether the change was tried before and how it turned out. Read-only.
